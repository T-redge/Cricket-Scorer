[package]
name = "rust-scorer-lib"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[profile.release]
lto = true
#opt-level = 's'

[dependencies]
wasm-bindgen = "0.2"
tokio-postgres = "0.7.16"
tokio = {version = "1.50.0", features = ["full"]}
