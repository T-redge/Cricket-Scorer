[package]
name = "api-server"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0"

db_access = {path = "../db_access/"}
thread_creator = {path = "../thread_creator/"}
tokio = {version = "1.50.0", features = ["full"] }
